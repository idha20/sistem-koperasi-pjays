services:
  - type: web
    name: sistem-koperasi-pjays
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn PJayS.PJayS.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: PJayS.PJayS.settings
      - key: PYTHON_VERSION
        value: 3.10.13
